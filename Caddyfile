{$DOMAIN:localhost} {
    # Frontend — Next.js
    handle {
        reverse_proxy frontend:3000
    }

    # Backend API — NestJS
    handle /api/* {
        reverse_proxy backend:3001
    }

    # Swagger docs
    handle /swagger* {
        reverse_proxy backend:3001
    }
}
